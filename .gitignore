.claude/settings.local.json
node_modules/

# CSVs de carga — dados operacionais, nunca versionar (ver CLAUDE.md)
*_carga.csv
prestacoes_contas_carga.csv
notas_liquidacao_import.csv

# ATENCAO: estes scripts tem a senha do banco EM TEXTO PURO na connectionString.
# Ignorados para nao vazarem no historico do git. A correcao de verdade e trocar
# a string literal por process.env.DATABASE_URL — depois disso podem ser versionados.
backfill_grupo_usuarios.js
carga_metas.js
carga_prazos.js
carga_sigpc.js
importar_nls.js

# Backup operacional gerado por script — nunca versionar (regra do CLAUDE.md)
nomes_antes.json
# A lista de reversao da renumeracao: 1.189 codigos de PC, dado operacional.
# Nao se perde se apagar — sai de _backup_parcial_num_20260813 comparado com o vivo.
reverter_renumeracao_*.json
# Lista de reversao da correcao em lote de processo_pc — dado operacional, recuperavel
# de _backup_processo_pc_20260813 comparado com o vivo.
reverter_processo_pc_*.json

# ── Os mapas da auditoria de 16/08/2026 ──────────────────────────────────────
# Dado operacional extraido do estoque oficial da CGE, nao codigo. O documento que
# os explica (AUDITORIA_SIGPC_2026-08-16.md) ESTA versionado; os dados, nao.
#
# ⚠️ MAPA_PARCIAL_SIGEF.csv e a entrada do renumerar_sigef.js — 8.998 linhas,
# chave codigo_pc -> parcial_sigef. Sem ele o script nao roda, e ele NAO se
# reconstroi a partir do banco: o numero do SIGEF nao existe em coluna nenhuma
# (conferido em 16/08). Se perder, e reimportar a planilha da CGE.
MAPA_PARCIAL_SIGEF.csv
TRS_AFETADAS_176.csv
trs_renumerar.csv
contagem.csv

# ⚠️ A FONTE da auditoria: o estoque oficial da CGE (45 MB) e as planilhas dos
# grupos. Dado operacional bruto, com nome de entidade, CNPJ e valor.
# O MAPA_PARCIAL_SIGEF.csv sai DAQUI: se ele se perder, e este arquivo que o
# reconstroi (aba "Parcial", colunas NR. PC -> Parcial).
#
# ⚠️ SAO PADROES NOMEADOS, DE PROPOSITO — nao ha regra `*.xlsx` aqui (decisao do
# Richard, 16/08/2026). Consequencia a lembrar: planilha NOVA com outro nome NAO
# e ignorada sozinha. Antes de `git add -A`, conferir `git status`. Uma planilha
# desta pasta no historico e dado de entidade e CNPJ publicado, e so sai
# reescrevendo a arvore.
ESTOQUE FCEE OFICIAL DA CGE.xlsx
ESTOQUE_FCEE_OFICIAL_DA_CGE.xlsx
GRUPO 1 - *.xlsx
GRUPO 2 - *.xlsx
GRUPO 3*.xlsx
RECUPERAR_MENOS1.csv

# ── Dado operacional de 16/08/2026 — nunca versionar ─────────────────────────
# Listas de PC com entidade, CNPJ e valor. Os documentos que as explicam
# (EXECUCAO_16-08.md, REVISAO_LOTE_2026-08-16.md) ESTAO versionados; os dados, nao.
PCS_AUSENTES_*.csv
PCS_NO_CONTROLE_INTERNO.csv
CI_PROCESSOS_SEM_BASE.csv
DIVERGENCIAS_21_RESOLVIDAS.csv
# Lista de reversao da gravacao de 16/08 — recuperavel dos backups no banco.
reverter_exec_*.json
reverter_frente2_*.json

# Listas da triangulacao da frente 2 — dado operacional
FRENTE2_*.csv

# Listas por analista do que falta confirmar no C.I. — dado operacional
CI_PENDENTE_POR_ANALISTA/

# Lista de reversao das 87 — recuperavel do _backup_87livres_20260816
reverter_87livres_*.json

reverter_soltas_*.json

# Lista de reversao da data real de baixa do SIGEF (27/08/2026) — 3.466 chaves com o
# valor anterior de cada PC. Dado operacional; o script que a gera ESTA versionado.
# ⚠️ Sao DOIS nomes de proposito — o `_DRYRUN` e o da gravacao —, e o padrao cobre os dois.
reverter_data_baixa_sigef_*.json

# Lista de reversao do status do SIGEF (27/08/2026) — as mesmas 3.466 chaves, com o
# rotulo anterior de cada PC. Dado operacional; o script que a gera ESTA versionado.
reverter_sigef_status_*.json

# Lista de reversao da coluna sigef_declaracao (27/08/2026). O script que a gera ESTA
# versionado; a reversao, nao.
reverter_sigef_declaracao_*.json

# ── Os relatorios de 25/08/2026 — dado operacional, nunca versionar ──────────
# Planilhas e PDFs com ENTIDADE, CNPJ, VALOR e NOME DE ANALISTA, PC a PC.
# Sao 5 arquivos e somam 5,4 MB. O que os EXPLICA pode ser versionado (um .md);
# os dados, nao.
#
# ⚠️ PADROES NOMEADOS, e nao `*.xlsx` — a decisao de 16/08/2026 continua valendo, e o
# aviso dela tambem: planilha NOVA com outro nome NAO e ignorada sozinha. Antes de
# `git add -A`, conferir `git status`.
#
# ⚠️ E A EXTENSAO E EXPLICITA, e nao `NOME.*`: assim um `DIAGNOSTICO_DIFERENCA_CGE.md`
# que explique o diagnostico continua podendo ser versionado, que e o padrao desta pasta
# (o AUDITORIA_SIGPC_2026-08-16.md esta no git; os CSVs dele, nao).

# Conferencia da migracao do C.I. — 2.144 PCs da planilha dos tecnicos
CONFERENCIA_MIGRACAO_CI_*.xlsx

# Diagnostico da diferenca apontada pela CGE — 390 parcelas do G2, PC a PC
DIAGNOSTICO_DIFERENCA_CGE.xlsx
DIAGNOSTICO_DIFERENCA_CGE.pdf

# Apuracao para a CGE — 3.618 PCs com valor, entidade, processo e NL
PCS_BAIXADAS_ATE_*_CGE.xlsx
PCS_BAIXADAS_ATE_*_CGE.pdf

# ⚠️ Os arquivos de LOCK do Excel — nascem quando alguem abre a planilha e somem ao fechar.
# Um deles apareceu em 25/08 no `git status` justamente enquanto o relatorio era conferido.
# Nao sao dado nem codigo: sao lixo temporario, e entram no repositorio sem ninguem notar.
~$*

# O diagnostico dos 16 processos do C.I. — traz entidade e nome de analista
DIAGNOSTICO_CI_16_PROCESSOS_*.md

# ⚠️ CSV de diagnostico gerado NA RAIZ — a familia inteira, nao os nomes de um dia so.
# Estes arquivos nascem de script de leitura contra o banco e saem com dado operacional
# NOMINAL: entidade, processo SGPe, NL, valor e o nome do analista, linha a linha. Em 26/08
# o CRUZAMENTO_SIGEF.csv (4.464 linhas) e o PCS_BAIXADAS_POR_ANALISTA.csv (4.063) apareceram
# soltos no `git status` — um `git add .` os levaria para o historico, de onde nao saem mais.
#
# A barra INICIAL ancora na raiz de proposito: e ali que os scripts escrevem. Um CSV dentro
# de uma pasta de codigo continua visivel, que e o que se quer.
#
# ⚠️ O PADRAO E POR PREFIXO, e a proxima rodada vai inventar outro prefixo. Antes de
# `git add -A`, conferir `git status` — vale aqui a mesma advertencia das planilhas acima.
/CRUZAMENTO_*.csv
/PCS_*.csv

# Lista de reversao da dispensa (28/08/2026) — os valores anteriores dos sete cadastros.
reverter_dispensa_*.json
